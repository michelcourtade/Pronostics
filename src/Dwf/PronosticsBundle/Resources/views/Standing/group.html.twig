{% extends '::base.html.twig' %}

{% block body -%}
    <ul class="breadcrumb">
    <li><a href="{{ path('events') }}">Accueil</a></li>
    <li><a href="{{ path('event_home', {'event': event.id }) }}">{{ event.name }}</a></li>
    <li><a href="{{ path('standings_event', {'event': event.id }) }}">{{ event.name }} - Classement</a></li>
    <li>Groupe {{ group.name }}</li>
    </ul>
<div class="page-header">
    <h1>{% if event.getWebPath %}<img src='/{{ event.getWebPath }}' />{% endif %} {{ event.name }} - Classement</h1>
</div>
{% if group %}
<div class="page-header">
    <h1>Classement groupe {{ group.name }}</h1>
</div>
{% include "DwfPronosticsBundle:Standing:standing.html.twig" %}{% else %}
<p>Pas de groupe trouvé</p>
{% endif %}
{% endblock %}
